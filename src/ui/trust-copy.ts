/**
 * Centralized trust copy — single source for all trust-related strings.
 * Import and reuse these to keep messaging consistent across popup,
 * onboarding, dashboard, and sandbox surfaces.
 */

export const TRUST_LOCAL =
  'Scans happen locally in your browser — nothing leaves your device';

export const TRUST_NO_STORAGE =
  'No prompt text is ever stored or transmitted';

export const TRUST_METADATA =
  'Only metadata (timestamps, categories, counts) is saved for your dashboard';

export const TRUST_FOOTER =
  'Local-only scanning. No prompt storage. Metadata only.';

export const TRUST_SANDBOX =
  'Test detection locally. No data leaves your browser.';

export const TRUST_FULL =
  'Sunbreak runs entirely in your browser. Your prompts are never stored, transmitted, or shared. Only anonymous metadata (finding counts, categories, timestamps) is kept for your personal dashboard.';
